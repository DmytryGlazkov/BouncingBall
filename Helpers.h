#pragma once

static int ScreenWidth	= 400;
static int ScreenHeight = 600;

static int Gravity = 3;
static int Bounce = 20;

const int PlatformsCount = 15;
static int PlatformWidth = ScreenWidth / 4;

static int Score = 0;
static bool Life = true;